name: Ping Next.js Cron

on:
  schedule:
    - cron: "*/5 * * * *"  # every 5 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Call cron endpoint
        run: curl -sS https://7b-store.vercel.app/api/cron
